(library
 (name polaris)
 (libraries
  readline 
  menhirLib
  unix
  str
  pcre
  threads))

(menhir
 (modules parser))

(env
  (dev
    (flags 
      (:standard -warn-error -A -w -27 -w -33 -warn-error +8)
    )))

(include_subdirs unqualified)